spring:
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate # Specifies how Hibernate should interact with existing tables
server:
  port: 5000
jwt:
  secret: ${JWT_SECRET}